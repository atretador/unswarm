# Deepwork state (git-local, OpenCode-readable via .ignore)
.slim/deepwork/

# Node
node_modules/
dist/
*.local

# .NET build artifacts
bin/
obj/

# Go build artifacts
agent/unswarm
agent/agent
unswarm-agent
# Built binaries must never be committed
**/unswarm
**/agent

# Env
.env
.env.*
!.env.example
unswarm.db
.tmp-ddcheck/
cli/unswarm
